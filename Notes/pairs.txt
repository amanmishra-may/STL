vector<pair<int, int>> vec = {{1, 5}, {2, 3}, {5, 6}};
    pair<string, int> p = {"Aman", 3};
    pair<int, pair<char, int>> pa = {1, {'a', 3}};

    cout << p.first << endl;
    cout << pa.second.first << endl;
    cout << pa.second.second << endl;

    for (pair<int, int> p : vec)
    {
        cout << p.first << " " << p.second;
    }
    cout << endl;

    vec.push_back({1, 2});  // insert already created pair
    vec.emplace_back(4, 5); // in-place objects create at the time of insertion

    for (auto p : vec)
    {
        cout << p.first << " " << p.second;
    }
    cout << endl;